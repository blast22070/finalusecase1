apiVersion: v1
kind: Service
metadata:
  name: ems-app-service
  labels:
    app: ems-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: ems-app
